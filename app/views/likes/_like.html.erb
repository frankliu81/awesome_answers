<%# the line below is refactored out into user_like in the controller %>
<%# like =  @question.like_for(current_user) %>
<%# if user_signed_in? && like %>
<% if user_like %>
  <%= link_to "Un-like", question_like_path(@question, user_like), method: :delete, remote: true %>
<% else %>
  <%= link_to "Like", question_likes_path(@question), method: :post, remote: true %>
<% end %>
<%= (@question.likes.count) %>
